services:
  - type: web
    name: quizflow-battle
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/battle-server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production